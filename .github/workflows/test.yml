name: test

on:
  push:
  pull_request:

jobs:
  install-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run install test
        run: bash tests/test_install.sh
